import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
  },

  textContainer: {
    fontSize: 30,
    color: '#464646',
    textAlign: 'center'
  },

  imageContainer: {
    width: '100%',
    height: 150,
    // height: '40%',
    // backgroundColor: 'red',
    objectFit: 'contain',
  }
});