import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    width: "100%",
    height: 50,
    backgroundColor: "#000",
    alignItems: "center",
    justifyContent: "center",
    elevation: 3,
    marginBottom: 20
  },

  textButton: {
    fontSize: 24,
    color: "#E5BF3C",
    fontWeight: "600"
  }
});
